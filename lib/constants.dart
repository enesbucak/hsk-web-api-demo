import 'package:flutter/material.dart';

//You need to take API key and write it here. Don't forget to encode it.
const String API_KEY = 'WRITE YOUT API KEY HERE';

const primaryColor = Color(0xFFf6f5f5);
const secondaryColor = Color(0xFF145374);
const ternaryColor = Color(0xFF00334E);
const accentColor = Color(0xFFee6f57);